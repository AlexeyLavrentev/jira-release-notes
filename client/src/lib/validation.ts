// Stub — full implementation in Plan 02
export type ValidationCategory = 'empty' | 'short' | 'placeholder' | 'valid';

export function validateReleaseNote(_note: string): ValidationCategory {
  return 'valid';
}
